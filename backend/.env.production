# Production Environment - Azure Deployment
NODE_ENV=production
PORT=8000
API_VERSION=v1

# Azure Database (will be updated when database is ready)
DATABASE_URL="postgresql://enameldbadmin:EnamelGeorgia2024!@enamel-georgia-db.postgres.database.azure.com:5432/postgres?sslmode=require"

# JWT Configuration (strong production secrets)
JWT_SECRET=prod_super_secret_jwt_key_2024_enamel_georgia_azure
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration (frontend + Azure domain)
CORS_ORIGIN=https://cool-machine.github.io,https://enamel-georgia-api.azurewebsites.net

# Stripe Configuration (test mode for now)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=uploads

# Rate Limiting (more restrictive for production)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=50

# Logging
LOG_LEVEL=warn